# Baked into the Vite production build automatically.
# Do NOT add to .gitignore — this file is intentionally committed.
# For local dev, Vite proxies /api to localhost:8000 (see vite.config.ts).
VITE_API_BASE=https://pdf-editor-jcx7.onrender.com/api
